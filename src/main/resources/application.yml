# application.yml
server:
  port: ${PORT:8080}
  servlet:
    context-path: "/cafeteria"

spring:
  application:
    name: cafeteria
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cafeteria-management}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

# swagger-ui and api-docs custom path
springdoc:
  swagger-ui:
    path: "/swagger-ui"
  api-docs:
    path: "/api-docs"